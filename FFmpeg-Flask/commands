ffmpeg -video_size 1920x1080 -framerate 30 -i /dev/video0 -preset veryfast -sc_threshold 0 -g 60 -f hls -hls_segment_type mpegts -hls_segment_filename data%02d.ts -hls_time 2 stream.m3u8
#^Must be run inside stream folder

sudo gunicorn -b 0.0.0.0:80 --timeout 0 server:app
